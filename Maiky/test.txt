start
	def user null
	def res string "result: "

	while *user != -1 do
		input string user

		if *user != -1 then
			mod res string *res + *user + " "
		end

		else
			print "encerrando input de usuario..." newl newl
		end
	end

	print *res
end